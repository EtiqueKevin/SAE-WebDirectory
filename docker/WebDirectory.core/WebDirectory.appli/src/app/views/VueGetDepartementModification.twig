{% extends "Index.twig" %}

{% block auth %}

    {% if connecte %}

        <a href="{{url_for('getRegister', {},{})}}"><p>CREATION UTILISATEUR</p></a>
        <a href="{{url_for('logout', {},{})}}"><p>DECONNEXION</p></a>

    {% else %}

        <a href="{{url_for('getAuth', {},{})}}"><p>CONNEXION</p></a>

    {% endif %}

{% endblock %}


{% block content %}
    <div id="card">
        <h1 class="titre">Modification d'un département</h1>
        <form action="/departement/modification" method="post" id="form-new-departement">
            <div><label for="nom"></label><input type="text" id="nom" name="nom" placeholder="Nom" value="{{departement.nom}}"></div>
            <div><label for="etage"></label><input type="number" id="etage" name="etage" placeholder="Étage" value="{{departement.etage}}"></div>
            <div><label for="description"></label><textarea id="description" name="description" placeholder="Description">{{departement.description}}</textarea></div>
            <input type="number" name="id" value="{{ departement.id }}" style="display: none">
            <input type="hidden" name="csrf" value="{{ csrf }}">
            <br>
            <button  type="submit">Modifier</button>
        </form>
    </div>
{% endblock %}