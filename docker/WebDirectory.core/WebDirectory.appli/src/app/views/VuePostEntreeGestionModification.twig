{% extends "Index.twig" %}

{% block auth %}

    {% if connecte %}

        <a href="{{url_for('getRegister', {},{})}}"><p>CREATION UTILISATEUR</p></a>
        <a href="{{url_for('logout', {},{})}}"><p>DECONNEXION</p></a>

    {% else %}

        <a href="{{url_for('getAuth', {},{})}}"><p>CONNEXION</p></a>

    {% endif %}

{% endblock %}


{% block content %}
    <div id="home">
        <p>Entree bien modifié : {{ args['nom'] }} {{ args['prenom'] }}</p>

    </div>
{% endblock %}